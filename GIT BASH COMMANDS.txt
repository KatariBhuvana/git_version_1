 $ git init
 
 $ ls -lrt
 
 $ git status
 
 # if it is asking who are you 
 $ git config --global user.name
 $ git config --global user.email
 
 #if it specifies and/or then we need to add file
 $ git add file name
 
 $ git commit
 
 #after giving some content in home page 
 
 $ touch style.css
 
 $ touch script.js
 
 $ git add -A
 
 $ git status
 
 $ git commit -m "updated HTML and CSS and JSS file"
 
 $ git checkout file name
 
 $ git checkout -f 
 
 $ git log - to see what are the changes done 
 
 $ clear - it will clear off everything and gives you screen clear but still you will have the transaction 
 
 $ git status
 
 $ git diff - it will show what changes have been done
 
 $ git add -A
 
 $ git diff --staged
 
 $ git commit -a -m "skipped the stagging area"
 
 $ git commit
 
 $ git status
 
 $ touch waste.html
 
 $ git add -A
 
 $ git commit -m " added a dummy file "
 
 $ git rm -- cached waste.html
 
 $ git status
 
 $ git rm waste.html
 
 $ git add -A
 
 $ git status
 
 
 
 
 
 
 
 
 